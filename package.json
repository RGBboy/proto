{
  "name": "proto",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "author": "RGBboy",
  "scripts": {
    "dev": "ecstatic ./static --port 9966 & chokidar 'src/*.elm' -c 'npm run build:client'",
    "build:client": "elm-make ./src/Main.elm --output ./static/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {},
  "devDependencies": {
    "chokidar-cli": "^1.2.0",
    "ecstatic": "^3.1.1",
    "elm": "^0.18.0"
  },
  "license": "NO LICENCE"
}
